<footer class="s-footer">
  <div class="s-footer__inner row">
    <div class="s-footer__brand column large-4 medium-full">
      <span class="s-footer__logo-img" role="img" aria-label="{{ settings[lang].siteName }}"></span>
    </div>
    <div class="s-footer__col column large-3 medium-4 mob-full">
      <p>{{ contact[lang].address.street }}<br>{{ contact[lang].address.zip }} {{ contact[lang].address.city }}<br>{{ contact[lang].address.country }}</p>
    </div>
    <div class="s-footer__col column large-3 medium-4 mob-full">
      {% if contact[lang].emails.general %}<a href="mailto:{{ contact[lang].emails.general }}">{{ contact[lang].emails.general }}</a>{% endif %}
      {% if contact[lang].emails.press %}<a href="mailto:{{ contact[lang].emails.press }}">{{ contact[lang].emails.press }}</a>{% endif %}
      {% if contact[lang].emails.careers %}<a href="mailto:{{ contact[lang].emails.careers }}">{{ contact[lang].emails.careers }}</a>{% endif %}
    </div>
    <div class="s-footer__col column large-2 medium-4 mob-full">
      {% for s in settings[lang].social %}<a href="{{ s.url }}" rel="noopener" target="_blank">{{ s.label }}</a>{% endfor %}
    </div>
  </div>
</footer>
